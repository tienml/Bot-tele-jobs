python-telegram-bot[job-queue]==21.9
requests==2.32.3
beautifulsoup4==4.14.2
lxml==6.1.1
# Giả lập TLS fingerprint của Chrome để qua Cloudflare của ITviec.
# requests dùng OpenSSL nên fingerprint khác trình duyệt và bị chặn thẳng.
curl_cffi==0.7.4
# Playwright dùng làm fallback khi curl_cffi bị Cloudflare JS-challenge chặn.
# Sau khi cài pip, cần chạy thêm: python -m playwright install chromium --with-deps
playwright==1.50.0
